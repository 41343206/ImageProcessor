# ImageProcessor - 新增功能說明

## 功能概述

本次更新為 ImageProcessor 應用程式新增了以下功能：

### 1. 矩形選取功能
- 在載入圖片後，使用者可以用滑鼠拖曳選取矩形區域
- 選取時會顯示紅色虛線矩形框
- 最小選取尺寸為 5x5 像素

### 2. 2倍放大顯示
- 當選取矩形完成後，選取的區域會自動：
  - 從原始圖片中擷取
  - 放大至原始大小的 2倍
  - 在新視窗中開啟

### 3. 畫筆塗改功能
- 新視窗提供繪圖工具，可以在放大的圖片上繪畫
- 可自訂筆刷顏色（透過顏色選擇對話框）
- 可調整筆刷寬度（1-50 像素）
- 支援即時繪圖

### 4. 另存新檔功能
- 編輯完成後可將圖片另存新檔
- 支援的格式：PNG、JPEG、BMP
- 提供儲存成功/失敗的提示訊息

## 使用方式

1. **開啟圖片**：點擊「開啟檔案」按鈕或使用 Ctrl+O
2. **選取區域**：在圖片上按住滑鼠左鍵並拖曳，畫出要選取的矩形區域
3. **放開滑鼠**：選取的區域會自動放大2倍並在新視窗中開啟
4. **繪圖編輯**：
   - 在新視窗中按住滑鼠左鍵移動即可繪圖
   - 點擊「筆刷顏色」按鈕可選擇顏色
   - 調整「筆刷寬度」數值框可改變筆刷粗細
5. **儲存檔案**：點擊「另存新檔」按鈕或使用 Ctrl+S

## 技術實作

### 新增檔案

1. **selectablelabel.h/cpp**
   - 自訂 QLabel 子類別，支援滑鼠拖曳選取功能
   - 處理滑鼠事件（按下、移動、放開）
   - 繪製選取框並發送選取信號

2. **editorwindow.h/cpp**
   - 編輯視窗主類別
   - DrawableLabel 類別：支援繪圖的 QLabel
   - 工具列：包含另存新檔、筆刷顏色、筆刷寬度控制

### 修改檔案

1. **imageprocessor.h/cpp**
   - 將 QLabel 改為 SelectableLabel
   - 新增 handleSelection 方法處理選取事件
   - 實作座標映射和圖片擷取邏輯

2. **ImageProcessor.pro**
   - 加入新的原始檔和標頭檔

3. **.gitignore**
   - 排除 Qt 建置產生的檔案

## 程式碼品質

- 加入邊界檢查，防止存取超出範圍
- 定義常數 MIN_SELECTION_SIZE 提升可維護性
- 防止除以零的保護機制
- 正確的座標映射（使用實際 pixmap 尺寸）
- 繪圖座標範圍限制（clamping）

## 建置需求

- Qt 5.x 或更新版本
- C++17 支援
- qmake 建置工具

## 建置指令

```bash
qmake ImageProcessor.pro
make
```

## 執行

```bash
./ImageProcessor
```
